DATABASE_URL=sqlite:///./parking_prediction.db
MODEL_PATH=../models_store/parking_model.pkl
DATA_DIR=../data

# ML integration (ml/ folder): use trained model for predictions
USE_ML_MODEL=true
# Optional: override paths (defaults: project_root/ml/models/parking_model.pkl and ml/data/processed)
# ML_MODEL_PATH=/path/to/ml/models/parking_model.pkl
# ML_DATA_DIR=/path/to/ml/data/processed
