# Environment Variables for Render Deployment
# Copy these to Render Dashboard → Backend Service → Environment

# MongoDB Atlas Connection (REQUIRED)
DATABASE_URL=mongodb+srv://parking_admin:jay123@cluster-parking-system.uyhmitw.mongodb.net/parking_db?retryWrites=true&w=majority

# ML Model Settings (REQUIRED)
USE_ML_MODEL=true

# Security Key (REQUIRED - change to random string in production)
SECRET_KEY=your-super-secret-random-key-change-this-in-production

# Optional: Python version (Render auto-detects from runtime.txt)
# PYTHON_VERSION=3.11.0
